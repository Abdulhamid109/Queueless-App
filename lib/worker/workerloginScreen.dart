import 'package:flutter/material.dart';

class Workerloginscreen extends StatelessWidget {
  const Workerloginscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Worker Login Screen!"),
      ),
    );
  }
}
