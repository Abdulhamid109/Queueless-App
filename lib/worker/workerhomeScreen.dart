import 'package:flutter/material.dart';

class Workerhomescreen extends StatelessWidget {
  const Workerhomescreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Wokers home screen"),
      ),
    );
  }
}